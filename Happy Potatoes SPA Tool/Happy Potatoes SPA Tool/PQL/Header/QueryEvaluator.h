#include <string>
#include <vector>
#include "../Header/ParseResult.h"

class QueryEvaluator
{
public:

    std::vector<std::string> startEvaluator(ParseResult);
	QueryEvaluator();
	~QueryEvaluator();
};

